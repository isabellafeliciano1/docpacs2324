/*function getMultipleRandom(colors, num) {
    const shuffled = [...colors].sort(() => 0.5 - Math.random());

    return shuffled.slice(0, num);
}

let colors = [
    'Red', 'Green', 'Blue', 'Purple', 'Yellow', 'Black'
];

color = getMultipleRandom(colors, 2);
console.log(color);
console.log(color.indexOf('Blue'))
if (color.indexOf('Blue') != -1 || color.indexOf('Green') != -1) {
    console.log("Im Grue Dabudi Dabudie")
}
else if (color.indexOf('Black') !== -1 && color.indexOf('Yellow') != -1) {
    console.log('Black and Yellow, Black and Yellow, Black and Yellow, Black and Yellow')
}
else {
    console.log("If I was literally anything else I would die")
};*/



let amongus = ['The', 'Impostor', 'Sus']
console.log(amongus)

for (var i = 0; 1 < Array.length; i++) {
    if (arr[i] == 'Impostor') {
        arr.splice(i, 1);
    }
}
console.log(amongus)